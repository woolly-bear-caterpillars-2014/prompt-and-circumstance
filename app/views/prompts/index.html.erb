<%= link_to "Create Prompt", new_prompt_path, class: 'btn' %>
<%= display_session_links %>

<ul class="prompts">
  <% @prompts.each do |prompt| %>
    <li>
      <%= link_to prompt.title, prompt  %>
    </li>
  <% end %>
</ul>
